<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema Base Eco-Friendly -->
    <style name="Base.Theme.Maxcargas" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cores Principais -->
        <item name="colorPrimary">@color/eco_green_primary</item>
        <item name="colorPrimaryVariant">@color/eco_green_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- Cores Secundárias -->
        <item name="colorSecondary">@color/eco_green_light</item>
        <item name="colorSecondaryVariant">@color/eco_green_dark</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/eco_green_dark</item>
    </style>

    <style name="Theme.Maxcargas" parent="Base.Theme.Maxcargas" />
</resources>